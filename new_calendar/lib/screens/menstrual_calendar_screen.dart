import 'package:flutter/material.dart';

class MenstrualCalendarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Calendário Menstrual")),
    );
  }
}
